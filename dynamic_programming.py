def value_function(env, theta=1e-5):
    """
    Find the optimal value function using an iterative strategy
    
    Returns a dictionary of the expected reward of being in each state
    """
    pass

# Use the value function
def Q_function(env, V, theta=1e-5):
    """
    Using the value function, greedily choose the optimal action in each state.
    This will the optimal policy for the environment.
    
    Returns a dictionary containing a policy table for the optimal action in each state.
    """
    pass